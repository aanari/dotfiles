# vim: set ft=sh:

xml() {
  xmllint -format - | if [ -t 1 ] && command -v pygmentize >/dev/null 2>&1; then
    pygmentize -l xml
  else
    cat
  fi
}

json() {
  python3 -m json.tool | if [ -t 1 ] && command -v coderay >/dev/null 2>&1; then
    coderay -json
  else
    cat
  fi
}
