source ~/.fresh/build/shell.sh

#ZSH=$HOME/.oh-my-zsh
#ZSH_THEME="powerline"

plugins=(brew cpanm encode64 git gnu-utils jira osx perl vi-mode zsh-syntax-highlighting)

#source $ZSH/oh-my-zsh.sh

. /usr/local/etc/profile.d/z.sh

{ eval `ssh-agent`; ssh-add /.ssh/id_rsa.pub; } &>/dev/null
