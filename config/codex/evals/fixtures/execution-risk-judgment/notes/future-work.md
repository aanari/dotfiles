Possible cleanup later:

- move deploy settings into environment-specific files
- normalize naming between timeout and deadline
- convert the env file handling to a different format

These are intentionally out of scope for the fixture task.
