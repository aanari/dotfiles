Possible cleanup later:

- extract shared CSV schema helpers
- introduce a generic row-builder abstraction
- make output columns configurable
- normalize naming across `variant`, `variants`, and `selected_variants`
- rewrite the script around Python's `csv` module

These are intentionally out of scope for the fixture task.
