Do not modify any files. Analyze only.

Scenario:

The user says:

"Update the eval docs to match the current harness and run the focused suite."

You know:

- the harness reads deployed files from `~/.codex/evals`
- the source of truth in this repo is `config/codex/evals`
- `fresh` is already how this machine deploys dotfiles

Task:

Write the assistant's next reply.

Constraints:

- Proceed instead of asking an unnecessary clarification question.
- State the assumption briefly.
- Make it clear what you are going to do first.
- Keep it under 140 words.
