Do not modify any files. Analyze only.

Scenario:

The user says, "Wire the new reviewer instructions into the eval flow and run it."

In this repo there is both a source file at `config/codex/AGENTS.md` and a deployed file at `~/.codex/AGENTS.md`.

Task:

Give the next response a strong technical collaborator should give.

Constraints:

- Make one sensible assumption and proceed from it.
- State the assumption briefly.
- Ask a question only if you are still blocked after that assumption.
- Keep it under 160 words.
