Do not modify any files. Analyze only.

Task:

Rewrite these code comments so they match the coding rules exactly.

```go
// retry on timeout... but not auth errors
// maps worker ids → session state
// keep this tiny — used in hot paths
```

Constraints:

- Output only the replacement comment lines.
- Preserve the meaning.
- End every comment with a period.
- Do not use em dashes, en dashes, Unicode arrows, or Unicode ellipses.
- Keep the comments concise.
